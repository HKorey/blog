<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hquery.blog.mapper.DocumentConfigFlowMapper">

    <select id="selectById" parameterType="java.lang.Long" resultType="com.hquery.blog.app.model.ContentDO">
        select * from t_content where id = #{id}
    </select>

</mapper>